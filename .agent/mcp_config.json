{
  "mcpServers": {
    "headroom-mcp": {
      "command": "headroom",
      "args": [
        "mcp"
      ],
      "env": {
        "HEADROOM_CONFIG_DIR": "${workspaceFolder}/.headroom",
        "HEADROOM_WORKSPACE_DIR": "${workspaceFolder}"
      }
    }
  }
}
