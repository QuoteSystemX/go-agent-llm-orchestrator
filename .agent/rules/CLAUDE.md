# CLAUDE.md — Prompt Library (Antigravity Kit Hub)

> This is the **source repository** for Antigravity Kit. Agents, skills, workflows, and templates
> are maintained here and distributed to target repositories via GitHub Actions.

## Project Purpose

This repository is the **prompt-library hub** — the single source of truth for the Agentic Kit
used across all QuoteSystemX repositories. It does NOT contain application code.

## Technical Standards

@.agent/KNOWLEDGE.md

## Agent & Skill System

Agents live in two locations:

| Location | Platform | Usage |
|----------|----------|-------|
| `.agent/agents/` | Antigravity (Gemini) | Source of truth — edit here |
| `.claude/agents/` | Claude Code | Auto-generated — do not edit directly |

Skills live in `.agent/skills/` and are embedded into `.claude/agents/` by the sync script.

**To regenerate Claude Code agents after editing `.agent/agents/` or `.agent/skills/`:**

```bash
python .agent/scripts/sync_claude_agents.py
```

## Architecture Reference

@.agent/ARCHITECTURE.md

## Key Rules for Claude Code

### 📎 Paperclip Heartbeat Protocol (MANDATORY)

**Every session with ANY agent MUST follow the Paperclip Heartbeat cycle (Skill: @[skills/paperclip]):**

1.  **Awareness**: Sync with current task and read `.agent/bus/` for context.
2.  **Lenses**: Apply Domain Lenses based on role category (Management, Engineering, QA, Infra).
3.  **Action**: Do not stop at planning; perform actionable work in the same heartbeat.
4.  **Reporting**: Every session MUST end with a **Progress Report** (Status, Blockers, Next Action).
5.  **Durable State**: Update task metadata and create child issues for delegated work.

### 🛠 Workflow Rules

1. **Never edit `.claude/agents/` directly** — files are generated by `sync_claude_agents.py`
2. **Modular Rules**: Edit Gemini rules in `.agent/rules/gemini/` and run `python .agent/scripts/compile_rules.py`
3. **Source of truth is `.agent/`** — all changes go there first
4. **For new agents**: create in `.agent/agents/`, then run sync script
5. **For skill updates**: edit in `.agent/skills/`, then run sync script
6. **Distribution**: push to `main` → GitHub Actions auto-distributes to target repos

## Commit Convention

Follow Conventional Commits (enforced by release-please):

- `feat(agent):` — new agent or skill
- `chore(agent):` — sync / maintenance
- `docs(agent):` — documentation updates
- `fix(agent):` — agent/skill bug fix
